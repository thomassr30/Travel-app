import React from 'react'
import { SafeAreaView, Text } from 'react-native'

const Gallery = () => {
  return (
    <SafeAreaView>
        <Text>Gallery</Text>
    </SafeAreaView>
  )
}

export default React.memo(Gallery)