import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    title: {
        fontSize: 13,
        color: '#4681A3',
    },
    container: {
        flexDirection: 'row',
        alignItems: 'center'
    },
    icon: {
        width: 40,
        height: 40
    }
})

export default styles;